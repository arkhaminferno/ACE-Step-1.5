# Copy to ".env" in the repo root (same folder as this file's parent).
# .env is gitignored — never commit secrets.
#
# FREESOUND_API_KEY = Freesound "Client secret / API key" (not Client ID)

FREESOUND_API_KEY=
